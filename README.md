# AI Portfolio (GitHub Pages + Jekyll)

이 저장소는 GitHub Pages에서 바로 배포할 수 있는 Jekyll 기반 AI 포트폴리오 사이트입니다.

## 로컬 실행

```bash
bundle install
bundle exec jekyll serve
```

브라우저에서 `http://localhost:4000`으로 접속하면 확인할 수 있습니다.
